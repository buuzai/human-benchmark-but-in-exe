# human-benchmark-but-in-exe
Why does every reaction time test run in a browser? Sites like Human Benchmark add browser overhead and rendering delay between your click and the actual measurement — meaning your score reflects your browser as much as it reflects you. I got tired of that, so I built a native .exe instead. 91 lines. Runs native. Open source, because why not.
